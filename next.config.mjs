/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "res.cloudinary.com",
      "mir-s3-cdn-cf.behance.net",
      "png.pngtree.com",
    ],
  },
};

export default nextConfig;
